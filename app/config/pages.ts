export const PAGES = {
	HOME: (locale: string) => `/${locale}`,
	PUBLICATIONS: (locale: string) => `/${locale}/publications`,
	PROJECTS: (locale: string) => `/${locale}/projects`,
	PHOTOS: (locale: string) => `/${locale}/photos`
}
